# Solana DEX BMV Bot - Production Environment Template

# --- RPC & Connection ---
# Primary RPC endpoints (HTTP and WebSocket)
RPC_PRIMARY_HTTP=https://api.mainnet-beta.solana.com
RPC_PRIMARY_WS=wss://api.mainnet-beta.solana.com
# Jito Bundler URL (Optional, uses default if empty)
# JITO_BUNDLER_URL=https://mainnet.block-engine.jito.wtf/api/v1/bundles

# --- Trading Configuration ---
TOKEN_MINT=So11111111111111111111111111111111111111112
OPENBOOK_MARKET_ID=
DRY_RUN_ENABLED=true

# --- Wallets & Secrets ---
# Comma-separated list of Base58 private keys or paths to keypair files
WALLET_KEYPAIRS=
# Fallback USDC wallet address
USDC_WALLET_3=

# --- Monitoring & Persistence ---
# Grafana admin password (MANDATORY: Use a strong, non-trivial password)
GRAFANA_PASSWORD=K9v#m2P&Lw8!xQz5*Tr4@nB7
# Database path (relative to /app in container)
DATABASE_PATH=bot_state.sqlite

# --- Application Internals ---
BOT_CONFIG_PATH=config.yaml
RUST_LOG=info
